# Dependency API Notes

Each file documents verified API facts for a specific package.

**File naming**: `<package-name>[v<version>].md`

**When to create**: After discovering API drift (wrong signatures, renamed
types, removed methods) or after learning a new package's API.

**When to read**: Before writing code that uses a package listed in
`CLAUDE.md # MEMORY → Documented Dependencies`.
