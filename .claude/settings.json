{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume|clear",
        "hooks": [
          {
            "type": "command",
            "command": "echo '## Session State (auto-loaded from docs/ai-docs/_status.md)' && (cat docs/ai-docs/_status.md 2>/dev/null || echo '(file not found — no active work in flight)')"
          }
        ]
      }
    ]
  }
}
